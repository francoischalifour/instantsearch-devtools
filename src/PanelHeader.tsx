import styled from 'styled-components';
import { AccordionHeader } from '@chakra-ui/core';

export const PanelHeader = styled(AccordionHeader)`
  text-transform: uppercase;
  font-size: 0.8rem;
  font-weight: bold;
`;
